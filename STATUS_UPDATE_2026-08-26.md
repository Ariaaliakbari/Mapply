# Mapply — status correction, 2026-08-26 (superseded)

**This file is out of date. Its "353 routes / 27 countries" figure was already stale once, then wrong again after that.** Do not use it for current counts.

The repo has been resynced twice since this was written. Current counts live in `README.md` — as of 2026-09-06 that's 1,479 routes across 138 countries. See README's "Note on undocumented expansion" section for the full history of what grew without being documented.

This file is kept only as a record of what changed in the 2026-08-26 session (three digital-nomad route upgrades and three new routes — see git history for that commit if you need the detail). It should not be treated as a live status file going forward; if another correction like this is needed, update README.md directly instead of adding a new dated file next to it.
